spring:
  application:
    name: restaurant-backend

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    database: postgresql
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

app:
  cors:
    allowed-origins: []

management:
  endpoints:
    web:
      exposure:
        include: health,info

info:
  app:
    name: restaurant-backend
    version: ${APP_VERSION:unknown}
